[package]
name = "cleanly-service"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
cleanly-core = { path = "../cleanly-core" }
cleanly-platform = { path = "../cleanly-platform" }
cleanly-apt = { path = "../cleanly-apt" }
cleanly-flatpak = { path = "../cleanly-flatpak" }
cleanly-snap = { path = "../cleanly-snap" }
cleanly-appimage = { path = "../cleanly-appimage" }
serde.workspace = true
toml.workspace = true
